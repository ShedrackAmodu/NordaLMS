# Core Django
Django==5.1.1
python-decouple==3.8
Pillow==10.2.0
whitenoise==6.6.0

# Django apps
django-crispy-forms==2.1
crispy-bootstrap5==0.7
django-filter==24.3
django-modeltranslation==0.19.7
django-model-utils==4.5.0

# Required for PDFs and models
reportlab>=4.0.0,<4.1.0
xhtml2pdf>=0.2.15

# Critical for LMS functionality
# Remove for space: django-jet-reboot django-storages django-anymail stripe gopay
# Only install what you absolutely need for core functionality

### Temporarily removed for free tier deployment ###
# django-jet-reboot==1.3.5  # Admin interface customization
# django-storages[boto3]==1.14.4  # For production file storage
# django-anymail[amazon_ses]==12.0  # Enhanced email
# stripe==8.8.0  # Payment processing
# gopay==2.0.1  # Alternative payment
# gunicorn==22.0.0  # Only needed if using gunicorn instead of WSGI
